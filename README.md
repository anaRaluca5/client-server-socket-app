# client-server-socket-app
Java client-server application using TCP/IP sockets. The client sends data to the server, which processes the request and returns the result. Demonstrates socket programming and inter-process communication.
